export const metadata = {
  title: "STIHL Store Albania",
  description: "Katalog produktesh STIHL me porosi në WhatsApp",
};

export default function RootLayout({ children }) {
  return (
    <html lang="sq">
      <body>{children}</body>
    </html>
  );
}
